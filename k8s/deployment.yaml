apiVersion: v1
kind: Namespace
metadata:
  name: kaiops-demo
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaiops-demo-app
  namespace: kaiops-demo
  labels:
    app: kaiops-demo-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kaiops-demo-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: kaiops-demo-app
    spec:
      containers:
        - name: app
          image: us-central1-docker.pkg.dev/project-3da8cb5f-328e-44d3-b7a/kaiops-demo-app/kaiops-demo-app:b6eb0f4f4e0bbb954b10e2e6f6d7e2b16e1cf265
          ports:
            - containerPort: 8080
          env:
            - name: APP_NAME
              value: "kaiops-demo-app"
            - name: APP_VERSION
              value: "v1.0.0"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: kaiops-demo-app
  namespace: kaiops-demo
  labels:
    app: kaiops-demo-app
spec:
  selector:
    app: kaiops-demo-app
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
